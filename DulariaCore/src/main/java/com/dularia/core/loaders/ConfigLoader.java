package com.dularia.core.loaders;

import com.dularia.core.DulariaCore;

@Deprecated
public class ConfigLoader {

    private DulariaCore plugin;

    public ConfigLoader(DulariaCore main) {
        this.plugin = main;
    }

    // im actually not sure what to add here yet, so ima make it deprecated for now ~Makoto

}
